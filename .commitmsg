Security: Fix premium bypass vulnerability

- Added hard checks in ExamPage: non-premium users are strictly blocked from rendering the exam and shown the PremiumModal instead.
- Added hard checks in TestPage: non-premium users are blocked from selecting topicId >= 2 and shown the PremiumModal instead.
- Prevents users from bypassing premium restrictions via direct URL navigation or sidebar links.
