Security Fix: login no longer auto-registers unknown users

- Login mode now shows error if phone number not found
- Only register mode creates new accounts
- Prevents accidental account creation via login form
