# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
dist
build

# Misc
.DS_Store
.env
.env.local

# Firebase Admin maxfiy kalit — HECH QACHON commit qilinmasin!
serviceAccount.json
*serviceAccount*.json
firebase-adminsdk*.json
.env.development.local
.env.test.local
.env.production.local

# IDEs
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Utility/Scratch scripts (ishlab chiqish uchun, GitHub'ga kerak emas)
scratch/
*.cjs
qoshimcha/
qoshimcha 2/
Qoshimcha 3/
ped mahorat/
tasviriy/
77777.docx
.env
fan/
scratch/
.vercel

# Tesseract OCR til modellari — lokal ish quroli, ilova kodi emas (~13MB).
# Kerak bo'lsa: https://github.com/tesseract-ocr/tessdata dan yuklab olinadi.
*.traineddata

# Lokal zaxira nusxalar va vaqtinchalik checkpoint/intermediate artefaktlar — repo'ga kerak emas
*.bak-*
*.bak-corrupt
pipeline/_chqbt_enrich_ckpt.json
pipeline/_chqbt_audit_report.json
pipeline/_lastraw.txt
src/data/firestore_backup_*.json
src/data/fix_report_*.md
# Savol fabrikasi: generatsiya loglari, pilot namunalari va korrektura checkpointlari
pipeline/_gen_*.log
pipeline/_pilot_*.json
src/data/.fix-progress-*.json
# Yangi fanlar manba to'plami (spetsifikatsiya PDF/txt, darsliklar, namuna rasmlar — ~100MB)
fan 2/

.vercel

# Nazariya konspekt qoralamalari (generatsiya + LLM javobi) — manba emas
pipeline/inbox/theory/

# nazariya generatsiyasi jurnali (har yurishda qayta yoziladi)
pipeline/_theory_gen.log

.env*

# MTT jismoniy tarbiya: audit hisobotlari va yurish loglari (lokal, katta)
pipeline/verify_report_mttjt_*.json
pipeline/_audit_mttjt_*.log
pipeline/_mttjt_*.log

# Rasm optimizatsiyasidan oldingi asl nusxalar (scripts/optimize-images.mjs)
image-backup/

# Kvota kuzatuvi namunalari (lokal diagnostika)
.kvota-namunalar.jsonl
